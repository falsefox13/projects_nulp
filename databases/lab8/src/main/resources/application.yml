spring:
  datasource:
    url: jdbc:mysql://localhost/jdbc_yashchuk?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: 1111
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: create
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8080

